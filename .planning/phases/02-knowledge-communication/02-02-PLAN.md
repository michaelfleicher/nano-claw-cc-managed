---
phase: 02-knowledge-communication
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/knowledge-brief.test.ts
autonomous: false
requirements:
  - COMM-02
  - COMM-03
  - COMM-04
  - COMM-05

must_haves:
  truths:
    - "Automated tests validate the knowledge brief structure and content requirements"
    - "Tests confirm all required XML sections exist in the global brief"
    - "Tests confirm handoff phrases and NEVER rules are present"
    - "Tests confirm WhatsApp formatting rules are included"
    - "Consultant reviews sample responses and confirms style accuracy"
  artifacts:
    - path: "tests/knowledge-brief.test.ts"
      provides: "Structural validation tests for the global knowledge brief"
      exports: ["describe('Knowledge Brief Structure')"]
  key_links:
    - from: "tests/knowledge-brief.test.ts"
      to: "groups/global/CLAUDE.md"
      via: "fs.readFileSync reads the knowledge brief and validates structure"
      pattern: "groups/global/CLAUDE.md"
---

<objective>
Write automated tests that validate the knowledge brief's structure and content, then have the consultant verify sample responses match their voice.

Purpose: Tests prevent knowledge brief drift (sections accidentally deleted, rules removed during editing). The checkpoint ensures the consultant validates the style and tone before going live.

Output: `tests/knowledge-brief.test.ts` (structural validation tests), consultant approval of sample responses
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-communication/02-RESEARCH.md
@.planning/phases/02-knowledge-communication/02-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 outputs -- the files being validated -->

groups/global/CLAUDE.md contains XML-tagged sections:
- `<identity>` -- assistant role and consultant representation
- `<services>` -- consulting services, engagement types, pricing
- `<communication_style>` -- tone, few-shot examples, WhatsApp formatting rules
- `<boundaries>` -- topics to handle vs. hand off, handoff phrases
- `<rules>` -- 7 absolute NEVER rules

groups/whatsapp_template/CLAUDE.md contains:
- ## Project, ## Preferences, ## Important Context, ## Notes, ## History
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write structural validation tests for the knowledge brief</name>
  <files>tests/knowledge-brief.test.ts</files>
  <read_first>
    - groups/global/CLAUDE.md (the knowledge brief being tested -- need to see actual content)
    - groups/whatsapp_template/CLAUDE.md (the template being tested)
    - .planning/phases/02-knowledge-communication/02-RESEARCH.md (anti-patterns and pitfalls to guard against)
  </read_first>
  <behavior>
    - Test: global CLAUDE.md contains `<identity>` XML section
    - Test: global CLAUDE.md contains `<services>` XML section with "Rate:" or pricing indicators
    - Test: global CLAUDE.md contains `<communication_style>` XML section with at least 3 `<example>` blocks
    - Test: global CLAUDE.md contains WhatsApp formatting rules (checks for "single asterisks", "NO double asterisks", "NO markdown links")
    - Test: global CLAUDE.md contains `<boundaries>` XML section with "Topics You CAN Handle" and "Topics You MUST Hand Off"
    - Test: global CLAUDE.md contains handoff phrases ("Let me check with" and "will want to handle personally")
    - Test: global CLAUDE.md contains `<rules>` XML section with all 7 NEVER rules
    - Test: global CLAUDE.md does not contain double asterisk bold patterns (**text**) outside of rules explaining what NOT to do
    - Test: global CLAUDE.md is under 3,000 tokens (rough estimate: character count / 4 < 3000)
    - Test: client template contains required sections (## Project, ## Preferences, ## Important Context, ## History)
  </behavior>
  <action>
Create `tests/knowledge-brief.test.ts` using vitest. Import `fs` and `path` from Node.js.

Read the knowledge brief file with:
```typescript
const briefPath = path.join(process.cwd(), 'groups', 'global', 'CLAUDE.md');
const brief = fs.readFileSync(briefPath, 'utf-8');
```

Write a `describe('Knowledge Brief Structure')` block with individual `it()` tests for each behavior listed above. Use simple string matching (`brief.includes()`, `brief.match()`, regex) -- no need for complex parsing.

For the token count estimate test, use: `Math.ceil(brief.length / 4) < 3000` as a rough approximation. The goal is to catch knowledge brief bloat early.

For the "no unintentional double asterisks" test, use a regex that finds `**` patterns that are NOT inside a rule explaining what not to do (e.g., skip lines containing "NO double asterisks").

Write a separate `describe('Client Template Structure')` block that reads `groups/whatsapp_template/CLAUDE.md` and validates required sections exist.

Use `beforeAll` to read files once and share across tests within each describe block.
  </action>
  <verify>
    <automated>npx vitest run tests/knowledge-brief.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/knowledge-brief.test.ts exists and is non-empty
    - File imports from "vitest" (describe, it, expect)
    - File reads "groups/global/CLAUDE.md" via fs.readFileSync
    - File contains describe('Knowledge Brief Structure') block
    - File contains at least 8 individual test cases (it() calls)
    - File contains describe('Client Template Structure') block
    - File contains token count estimate test with threshold 3000
    - All tests pass when run with `npx vitest run tests/knowledge-brief.test.ts`
  </acceptance_criteria>
  <done>Structural validation test suite passes, confirming the knowledge brief has all required sections (identity, services, communication_style, boundaries, rules), WhatsApp formatting rules, handoff phrases, NEVER rules, reasonable token count, and client template completeness</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consultant reviews knowledge brief and sample responses</name>
  <files>groups/global/CLAUDE.md, groups/whatsapp_template/CLAUDE.md</files>
  <action>
Checkpoint: The consultant reviews the knowledge brief and client template created by Plan 01, then customized by filling in real content. No automated action -- this is a human verification gate.

The consultant must:
1. Replace all [bracketed placeholders] in groups/global/CLAUDE.md with real information
2. Verify the structure, tone, and rules match their expectations
3. Confirm tests still pass after edits
  </action>
  <verify>
    <automated>npx vitest run tests/knowledge-brief.test.ts 2>&1 | tail -5</automated>
  </verify>
  <how-to-verify>
    1. Open `groups/global/CLAUDE.md` and review the structure
    2. Replace all `[bracketed placeholders]` with your actual information:
       - Your name and assistant name
       - Your consulting services and pricing
       - 3-5 real WhatsApp message examples showing your communication style
       - Adjust tone descriptors to match how you actually write
       - Review and customize the topic boundary lists
    3. After filling in real content, run: `npx vitest run tests/knowledge-brief.test.ts`
       - All tests should still pass with your real content
    4. Review the handoff phrases -- do they sound like something you would say?
    5. Review the NEVER rules -- are there any additional rules needed?
    6. Check the client template at `groups/whatsapp_template/CLAUDE.md` -- does it capture the information you'd want for each client?
  </how-to-verify>
  <done>Consultant has reviewed and customized the knowledge brief with real content, tests still pass after edits, and consultant approves the brief for use</done>
  <resume-signal>Type "approved" if the brief structure works and you have filled in your real content, or describe what needs to change</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run tests/knowledge-brief.test.ts` passes all tests
2. Consultant has reviewed and customized the knowledge brief with real content
3. Knowledge brief maintains required structure after consultant edits (tests still pass)
</verification>

<success_criteria>
- Automated tests validate all structural requirements of the knowledge brief (COMM-02 through COMM-05)
- Tests serve as a guardrail against knowledge brief drift during future edits
- Consultant has reviewed and approved the brief structure and filled in real content
- The knowledge brief is ready for use with NanoClaw's agent runner
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-communication/02-02-SUMMARY.md`
</output>
